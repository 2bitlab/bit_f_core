# configModule

获取配置

配置都写在具体的 vue 文件内

在路由加载前，会通过 store 加载当前路由的系统配置和用户配置

把三个配置进行叠加

配置设置时都有两个选择

1、更新基本配置

基本配置级是按照 `[组件名] + [变量名]` 做 key

2、更新当前页配置

页面级是按照 `[当前的路由] + [组件名] + [变量名]` 做 key

组件层的 `config` 计算规则：
